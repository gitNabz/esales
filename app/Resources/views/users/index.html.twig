{% extends 'base.html.twig' %}

{% block title %} Liste des utilisateurs {% endblock %}

{# app/Resources/views/users/index.html.twig #}

{% block body %}

{% block stylesheets %}
<style>
	body {
		background-color: #005831;
	}
</style>
{% endblock %}
	<table>
		<thead>
			<tr>
				<th>ID</th>
				<th>Nom</th>
				<th>Prénom</th>
				<th>Date de naissance</th>
			</tr>
		</thead>
		<tbody>
			{% for user in users %}
				<tr>
					<td><a href="{{ path('users.show', {"id" : user.id}) }}">{{ user.id }}</a></td>
					<td>{{ user.lastname }}</td>
					<td>{{ user.firstname }}</td>
					<td>{{ user.birthdate|date("d/m/Y") }}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}